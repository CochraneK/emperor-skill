# 变更记录

按时间倒序。日期为提交日（2026-09）。

## 2026-09-13

- **solo 排名报告升级为三级排序维度**（`2b01c6a`）——`solo-ranking.html` 改为八节：全体总榜（主键总分，同分按时间序）/ 分朝代榜 5 张 / 分模块榜 7 张 / 分档 / 9 类困局适配排行 / 结构约束极差 / 亡国危局带 / 各朝构成量。修正第六节口径错误（「全库均分」原用各人均分再平均，与第五节合并口径不一致，差 0.1）。新增 `build_module_map.py` 与 `modules_solo.json`（94 包模块映射）。
- **新增 78 帝总排名报告**（`e0e7e94`）——跨帝王聚合：总分榜、分档一览、类型适配 Top5·Bottom5、极差对照、亡国危局分数带。生成器 `eval_solo_rank.py`。
- **帝王包扩至 94 个**（`b28ab7f`）——新增东汉 13 帝（`skills/donghan/`）与隋 3 帝（`skills/sui/`），均过 6/6 门禁。**注意**：处境全集仍为 78 条，这 16 包尚未纳入 solo 推演。
- **solo 报告与总目录改为按时间顺序**（`0482a56`）——朝代先后 → 即位年 → 同年次序，依据新增的 `reign_order.json`（78 帝即位年表）。此前为按分数排列。
- **78 帝 solo 推演全量完成 + 全库交叉检验**（`f5bcd23`）——78 × 77 = 6006 条处置；`check_solo.py` 78/78 结构通过；`cross_check_solo.py` 结论：亡国危局档 78 人全落 39.0–51.3（零越界），守成/变法/权臣/储位四档人均极差 25.0–35.5，为亡国档 2.0–2.9 倍。
- **翻盘三条件效度检验 T0/T1 双时点对照**（`a03f9ce`）——诊断出评分卡**循环论证**：题面按「结局当口」写定，而三条件量的是即位时的结构存量，两时点不同源。报告见 `simulation/validity/`。
- **重构为三大块**（`36e6480`）——`skills/`（Skill 库）/ `simulation/`（模拟场）/ `assessment/`（人格测验）。

## 2026-09-12

- **唐宋帝王 Skill 入库**（`40ed6ed`）——唐 21 帝 + 宋 18 帝，按朝代分文件夹。
- **明清元帝王 Skill 入库**（`fc91ef5`）——明 16 + 清 12 + 元 11，按朝代分文件夹。仓库自蒸馏主项目（nuwa-skill）剥离，独立分发。
