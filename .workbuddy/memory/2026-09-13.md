# 2026-09-13 —— 排名报告三级排序 + 收尾体检

## 完成

- **solo 排名报告升级为三级排序维度**（`2b01c6a`）：`solo-ranking.html` 改八节 ——
  ① 全体总榜（主键**总分**降序，同分按时间序）② 分朝代榜 5 张（朝内名次）
  ③ 分模块榜 7 张（模块内名次；`donghan`/`sui` 标「尚未纳入」）④ 分档 ⑤ 9 类困局适配排行
  ⑥ 结构约束 vs 人定胜负 ⑦ 亡国危局带 ⑧ 各朝构成量。
- 新增 `_engine/build_module_map.py` → `modules_solo.json`：从 94 份 `SKILL.md` 的 frontmatter
  抽 `module` / `group` / `cn` / `era`，「模块」维度从此有权威字段（此前无此数据源）。
- **修正第六节口径错误**：该节「全库均分」原用「各人均分再平均」，与第五节的合并口径不同源，
  导致守成 69.6/69.5、变法 61.2/61.0 等各偏 0.1。改为统一的 `pooled_mean`，
  极差统一改称「人均极差」，表头写明两个口径不可混读。

## 独立校验（fresh-eyes worker，不复用生成器）

10 项里 9 项 ✅，抓到上述 1 处口径错误。另确认：78 文件 / 6006 条 / 每人恰 77；
总榜 78 行逐行比对 0 处不一致；13 张表 `<th>` 数 = 数据行 `<td>` 数；无 `{{`/`None`/`nan` 残留；
唐内第一李忱 4950、第二武则天 4898（差 52，无并列），全局 3 组同分均按 朝代→即位年 正确区分。

## 收尾体检（project-closeout-kit）

- **结构**：根目录干净（仅 README/LICENSE/.gitignore + 三个目录）；补 `AGENTS.md`（原缺，HIGH）。
- **跨机**：补 `requirements.txt`（诚实声明「无第三方依赖，仅需 Python ≥3.13」）；
  修 `SOLO_TASK_BRIEF.md` 中写死的本机解释器路径（含用户名，隐私 + 跨机双输）为可移植写法。
- **新增文档**：`HANDOFF.md`（环境/数据来源/当前卡点/已知坑/下一步）、`CHANGELOG.md`（9 次提交的版本叙事）。
- **新增项目记忆**：`.workbuddy/memory/`（本仓随仓库分发，解决「记忆只在工作区、不随 repo 走」的断点）。
- **`.gitignore` 扩容**：原仅 Python 缓存 + OS 文件，补 `.env*`、密钥类、虚拟环境、临时文件。
- **顺手修掉体检脚本自身 bug**：`closeout_audit.py:180` 把 4 元组的 `PRIVACY_RULES`
  按 3 元组解包 → `ValueError`，隐私扫描整块崩溃。改为 `for rule_name, rx, sev, why in ...`。
