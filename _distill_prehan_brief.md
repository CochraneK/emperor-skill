# HISTORICAL BRIEF — 西汉以前帝王蒸馏任务书（已取代）

> ⚠️ 本文件原称“唯一规范来源”，该声明现已失效。自 2026-09-17 起，仓库级唯一审查正典为 [`NUWA_AUDIT_V2.md`](NUWA_AUDIT_V2.md)，执行规范为 [`WORKER_BRIEF_V2.md`](WORKER_BRIEF_V2.md)。本文件仅保留为早期批次的 provenance / spec-history 说明。

## 历史批次边界

本任务书曾覆盖西汉建立前的帝王 `-perspective` 建包，并规定当时的工程结构与展示登记方式。早期批次允许 research/references 可选，因此：**一个早期包只有 `SKILL.md`，本身不能证明它违反当时任务，也不能单凭结构推断其 Nuwa provenance。**

## 仍可沿用的内容原则

- 史料不足必须明确限制，不得把传说写成确定事实。
- 心智模型应有证据、适用边界与局限。
- 保留人物内部张力，不把矛盾强行调和。
- Expression DNA、角色边界与退出角色机制可继续作为产品工程层约束。
- 黑名单/低质量来源不能成为核心论据。

## 不再具有的权限

本文件不再能覆盖 Nuwa Audit v2，也不能凭固定模型数、关键词、目录结构、时间线条数、source ratio 或其他机械计数证明 Nuwa 合格。旧 checker 只属于 L4/静态信号。

历史包进入 recovery 时：先判 L1 provenance，再判 L2 evidence，再做 L3 semantic audit，最后才是 L4 engineering。没有独立 research 的包应根据真实历史和证据状态决定 `RESEARCH-GAP` / `RERESEARCH` / `REDISTILL` / `REVIEW`，而不是因为“旧任务允许”就自动 PASS。

> 原始完整任务书可从 Git history 恢复。本文件保留这一入口，同时消除“旧 brief 继续支配新 agent”的规范冲突。
