name: Nuwa L3 Semantic Review

on:
  push:
    branches: [main]
    paths:
      - 'skills/**/SKILL.md'
      - 'data/corpus-gap.json'
      - '_redo_tools/_audit_l3_semantic_similarity.py'
      - '.github/workflows/nuwa-l3.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: nuwa-l3-${{ github.ref }}
  cancel-in-progress: true

jobs:
  semantic-review-signals:
    if: github.actor != 'github-actions[bot]' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Compile scanner
        run: python -m py_compile _redo_tools/_audit_l3_semantic_similarity.py

      - name: Generate L3 review signals
        run: python _redo_tools/_audit_l3_semantic_similarity.py

      - name: Guard semantic meaning
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          p=Path('data/l3-semantic-review-queue.json')
          doc=json.loads(p.read_text(encoding='utf-8'))
          assert doc['status']=='GENERATED_REVIEW_SIGNALS_NOT_L3_VERDICTS'
          assert doc['policy']['automatic_pass_fail'] is False
          assert doc['summary']['packages_scanned'] > 0
          print(json.dumps(doc['summary'], ensure_ascii=False, indent=2))
          PY

      - name: Commit generated review queue
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -- data/l3-semantic-review-queue.json assessment/L3_SEMANTIC_QA.md
          if git diff --cached --quiet; then
            echo 'No L3 review-signal changes.'
            exit 0
          fi
          git commit -m 'chore: sync Nuwa L3 semantic review queue'
          git push
